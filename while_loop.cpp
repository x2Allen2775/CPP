#include <iostream>

/*int main() {
    //while loop=is a if statement but we can repeat that code potentially infinite no of times.
    //eg

    std::string name;
    while (name.empty()){//is code ka matlab hai jab tak vo string empty hai par vo infinitely tak repeat hoga till then string is not empty
        std::cout<<"Enter your name: ";
        std::getline(std::cin, name);
    }
    std::cout<<"Hello "<<name;
    return 0;
}*/
/*#include <iostream>

int main() {
    //example of while loop
    while(1==1){
        std::cout<<"Help";
    }
    return 0;
}
*/
//an use of this while loop is to force the user to do something toocontinue with rest of the code...
