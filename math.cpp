#include <iostream>
#include <cmath>


int main() {
    double x=3;
    double y=4;
    double z;

    //to get the max out of two variables...
    //z= std::max(x,y);
    //std::cout<<z;
    //max or min ese karte hai and x, y ki jagah actual no bhi ho skate hai
    //for power function...
    //z=pow(2, 3);
    //std::cout<<z;
    //for sqrt
    //z=sqrt(9);
    //std::cout<<z;

    //for absolute value...
    //z=abs(-3);

    //z=round(x) for rounding up
    //for rounding up there is ceiling function
    //z=ceil(x);
    //similarly to rounding below
    //z=floor(x);
    std::cout<<z;
    return 0;
}

// for more function visit cplusplus.com/reference/cmath/
